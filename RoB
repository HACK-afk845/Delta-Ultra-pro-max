--[[
    ✨ Optimized, English Edition (Spam Removed)
]]

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local coreGui = game:GetService("CoreGui")
local tweenService = game:GetService("TweenService")
local runService = game:GetService("RunService")

-- [ 1. GUI Initialization ]
local coreGuiContainer = Instance.new("ScreenGui", coreGui)
coreGuiContainer.Name = "BaseModule_Interface"

local BaseFrame = Instance.new("Frame", coreGuiContainer)
BaseFrame.Size = UDim2.new(0, 450, 0, 450)
BaseFrame.Position = UDim2.new(0.5, -225, 0.5, -225)
BaseFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
BaseFrame.Visible = false
Instance.new("UICorner", BaseFrame)
Instance.new("UIStroke", BaseFrame).Thickness = 2

local ControlBtn = Instance.new("TextButton", coreGuiContainer)
ControlBtn.Size = UDim2.new(0, 110, 0, 50)
ControlBtn.Position = UDim2.new(0, 15, 0.5, 0)
ControlBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ControlBtn.Text = "MENU"
ControlBtn.TextColor3 = Color3.fromRGB(255, 0, 0)
ControlBtn.Draggable = true
Instance.new("UICorner", ControlBtn)

ControlBtn.MouseButton1Click:Connect(function() BaseFrame.Visible = not BaseFrame.Visible end)

local ScrollContainer = Instance.new("ScrollingFrame", BaseFrame)
ScrollContainer.Size = UDim2.new(1, -20, 1, -60)
ScrollContainer.Position = UDim2.new(0, 10, 0, 50)
ScrollContainer.BackgroundTransparency = 1
Instance.new("UIListLayout", ScrollContainer).Padding = UDim.new(0, 8)

local function AddButton(name, callback)
    local btn = Instance.new("TextButton", ScrollContainer)
    btn.Size = UDim2.new(1, -10, 0, 40)
    btn.BackgroundColor3 = Color3.fromRGB(45, 0, 0)
    btn.Text = name
    btn.TextColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", btn)
    btn.MouseButton1Click:Connect(callback)
end

-- [ 2. Features ]

AddButton("Open Script Again", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/HACK-afk845/Delta-Ultra-pro-max/refs/heads/main/RoB"))()
end)

AddButton("Clear HD Admin", function()
    for _, v in pairs(workspace:GetDescendants()) do
        if v.Name:lower():find("hdadmin") then v:Destroy() end
    end
end)

AddButton("VR7 Tool", function() 
    loadstring(game:HttpGet("https://raw.githubusercontent.com/VR7ss/OMK/refs/heads/main/VR7-ON-TOP"))() 
end)

print("System Ready - English Version")
